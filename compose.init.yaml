services:
  dnsblackhole:
    volumes:
      - type: bind
        source: ./docker/bootstrap-config.json
        target: /etc/dnsblackhole/config.json
        read_only: true
    command:
      - serve
      - --data-dir
      - /var/lib/dnsblackhole
      - --web-listen
      - 0.0.0.0:3000
      - --admin-password-file
      - /run/secrets/dnsblackhole_admin_password
      - --bootstrap-config
      - /etc/dnsblackhole/config.json
